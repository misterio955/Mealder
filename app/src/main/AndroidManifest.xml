<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ideo.mealder">

    <application
        android:allowBackup="true"
        android:appComponentFactory="android.support.v4.app.CoreComponentFactory"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MealViewPagerActivity" />
        <activity android:name=".activities.HomeScreenActivity" />
        <activity
            android:name=".activities.SignUpActivity"
            android:label="@string/title_activity_sign_up" />
        <activity
            android:name=".activities.MealRecyclerActivity"
            android:label="@string/title_activity_meal_recycler" />
        <activity
            android:name=".Utils.RecyclerListAdapter"
            android:label="@string/title_activity_recycler_list_adapter" />
        <activity
            android:name=".activities.MealDetailsActivity"
            android:label="@string/title_activity_meal_details" />
        <activity
            android:name=".activities.FavouritiesActivity"
            android:label="@string/title_activity_favourities" />
        <activity
            android:name=".activities.MyRecipesActivity"
            android:label="@string/title_activity_my_recipes"></activity>
    </application>

</manifest>